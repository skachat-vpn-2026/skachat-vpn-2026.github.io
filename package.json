{
  "name": "vpn-status",
  "version": "1.0.0",
  "private": true,
  "description": "Static VPN status monitoring site for GitHub Pages",
  "scripts": {
    "build": "node scripts/build.js",
    "start": "npm run build && npx --yes http-server . -p 8080 -c-1"
  },
  "engines": {
    "node": ">=18"
  }
}
